services:
  backend:
    build:
      context: .
      target: dev

    container_name: backend_dev

    ports:
      - "3000:3000"

    volumes:
      - .:/app
      - /app/node_modules

    env_file:
      - .env

    dns:
      - 8.8.8.8
      - 1.1.1.1